MESSAGE( STATUS "LINK PATH: " ${LIBRARY_OUTPUT_PATH}/ )

INCLUDE_DIRECTORIES( internal )

ADD_EXECUTABLE( test
    encode.cc )

LINK_DIRECTORIES( ${LIBRARY_OUTPUT_PATH}/ )

TARGET_LINK_LIBRARIES( test pcre pcrecpp )
